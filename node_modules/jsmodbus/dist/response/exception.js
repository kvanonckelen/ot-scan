"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.isExceptionResponseBody = isExceptionResponseBody;
const codes_1 = require("../codes");
const response_body_js_1 = __importDefault(require("./response-body.js"));
class ExceptionResponseBody extends response_body_js_1.default {
    get code() {
        return this._code;
    }
    get message() {
        return (0, codes_1.errorCodeToMessage)(this._code);
    }
    get meiType() {
        return this._meiType;
    }
    get byteCount() {
        if (this._meiType === undefined) {
            return 2;
        }
        else {
            return 3;
        }
    }
    get isException() {
        return true;
    }
    static fromBuffer(buffer) {
        const fc = buffer.readUInt8(0) - 0x80;
        if (!(0, codes_1.isFunctionCode)(fc)) {
            throw Error('InvalidFunctionCode');
        }
        if (fc === codes_1.FC.READ_DEVICE_IDENTIFICATION) {
            if (buffer.length >= 3) {
                const meiType = buffer.readUInt8(1);
                const meiExceptionCode = buffer.readUInt8(2);
                return new ExceptionResponseBody(fc, meiExceptionCode, meiType);
            }
            if (buffer.length >= 2) {
                const legacyExceptionCode = buffer.readUInt8(1);
                return new ExceptionResponseBody(fc, legacyExceptionCode);
            }
            throw Error('InvalidBufferLength');
        }
        const defaultExceptionCode = buffer.readUInt8(1);
        return new ExceptionResponseBody(fc, defaultExceptionCode);
    }
    static fromRequest(requestBody) {
        return new ExceptionResponseBody(requestBody.fc, requestBody.code);
    }
    constructor(fc, code, meiType) {
        const ignoreInvalidFunctionCode = true;
        super(fc, ignoreInvalidFunctionCode);
        this._code = code;
        this._meiType = meiType;
    }
    createPayload() {
        const payload = Buffer.alloc(this.byteCount);
        payload.writeUInt8(this._fc + 0x80, 0);
        if (this._meiType !== undefined) {
            payload.writeUInt8(this._meiType, 1);
            payload.writeUInt8(this._code, 2);
        }
        else {
            payload.writeUInt8(this._code, 1);
        }
        return payload;
    }
}
exports.default = ExceptionResponseBody;
function isExceptionResponseBody(x) {
    if (x instanceof ExceptionResponseBody) {
        return true;
    }
    else {
        return false;
    }
}
